{
  "name": "idol-tokuten-lab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node --check app.js && node --check scripts/build.js"
  }
}
